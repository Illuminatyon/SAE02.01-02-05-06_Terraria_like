/*package fr.iut.hev.root.view.Listener;

import javafx.beans.value.ChangeListener;
import javafx.beans.value.ObservableValue;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;

public class TileListener implements ChangeListener<Image> {

    private ImageView sprite;

    public TileListener(ImageView sprite) {
        this.sprite = sprite;
    }

    @Override
    public void changed(ObservableValue<? extends Image> observableValue, Image image, Image t1) {
        System.out.println("enter");
        int x = (int)sprite.getX(),y = (int)sprite.getY();
        System.out.println("x = " + x);
        System.out.println("y = " + y);
    }
}*/
